// Eldorado: AI-generated clothing designs, printed and shipped on demand in-house.

generator client {
  provider = "prisma-client-js"
  output   = "../generated"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Design {
  id        String   @id @default(cuid())
  prompt    String
  imageUrl  String
  orders    Order[]
  createdAt DateTime @default(now())
}

model Order {
  id                           String   @id @default(cuid())
  designId                     String
  design                       Design   @relation(fields: [designId], references: [id])
  size                         String
  quantity                     Int      @default(1)
  retailPriceAmountMinorUnits  Int
  currency                     String
  // Unknown until Stripe Checkout completes -- Stripe's hosted page collects this itself, so
  // the app never needs its own email/shipping form before starting checkout.
  recipientEmail               String?
  shippingName                 String?
  shippingLine1                String?
  shippingLine2                String?
  shippingCity                 String?
  shippingRegion               String?
  shippingPostalCode           String?
  shippingCountryCode          String?
  // PENDING_PAYMENT | PAID | IN_PRODUCTION | SHIPPED | CANCELED -- fulfillment is in-house and
  // manual, driven from the admin dashboard, not an external POD API.
  status                       String   @default("PENDING_PAYMENT")
  stripeCheckoutSessionId      String?  @unique
  trackingCarrier              String?
  trackingNumber               String?
  trackingUrl                  String?
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt

  @@index([designId])
}
